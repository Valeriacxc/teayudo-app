import 'package:flutter/material.dart';

class AppTheme {
  // Paleta clara
  static const Color celesteSuave = Color(0xFFE6F6FF); // fondo muy suave
  static const Color celesteAccento = Color(0xFF87CEFA); // acento celeste
  static const Color violetaMuySuave = Color(0xFFF3E8FF); // toque sutil

  // Paleta oscura
  static const Color negro = Color(0xFF000000);
  static const Color grisCasiNegro = Color(0xFF0F0F10);

  // Común
  static const Color textoBlanco = Colors.white;
  static const Color botonPanico = Colors.redAccent;
}

